@using WalletApp.Data.Models
@model Holder

<div class="dropdown">
    <button class="btn btn-sm text-muted" type="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="Holder actions menu">
        <i class="fas fa-ellipsis-v"></i>
    </button>
    <ul class="dropdown-menu dropdown-menu-end">
        <li>
            <a asp-page="/Admin/Holders/Edit"
               asp-route-key="@Model.HolderKey"
               class="dropdown-item">
                <i class="fas fa-edit me-1" style="width: 1em"></i>
                View / Edit
            </a>
        </li>
        <li>
            <a href="#"
               class="dropdown-item"
               data-bs-toggle="modal"
               data-bs-target="@($"#delete-holder-{Model.HolderKey}")">
                <i class="fas fa-trash-alt me-1" style="width: 1em"></i>
                Delete
            </a>
        </li>
    </ul>
</div>
<form method="post"
      class="modal"
      id="@($"delete-holder-{Model.HolderKey}")"
      tabindex="-1"
      data-ajax="true"
      data-ajax-method="post"
      data-ajax-url="?handler=DeleteHolder&amp;key=@Model.HolderKey"
      data-ajax-loading="#loader"
      data-ajax-success="$('#delete-holder-@Model.HolderKey').one('hidden.bs.modal', function(){ $('#holder-@Model.HolderKey').remove(); }).modal('hide');"
      data-ajax-failure="showToast(xhr.statusText)">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Delete @Model.Name</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>
                    Are you sure you want to delete this holder?
                </p>
                <button type="submit" class="btn btn-sm btn-danger text-uppercase">
                    Delete
                </button>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-sm btn-secondary text-uppercase" data-bs-dismiss="modal">
                    Cancel
                </button>
            </div>
        </div>
    </div>
</form>
